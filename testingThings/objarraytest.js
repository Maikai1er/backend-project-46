const children = {
  key: 'common',
  type: 'unchanged',
  value: 'false',
};

const newObj = {};

const thisKey = children.key;

const thisValue = children.value;

newObj[thisKey] = thisValue;

console.log(newObj);
