const obj = {
  key: null,
  type: 'root',
  children: [
    {
      key: 'common',
      type: 'unchanged',
      value: 'false',
    }],
};
const newObj = {};
const constructor = (object) => {
  const constructObject = (key, value) => ({ [key]: value });

};
constructor(obj);
console.log(obj);
